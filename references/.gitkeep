# Положи сюда свои записи открывания банки (любой аудио/видео-файл) и запусти
# `python train.py`, чтобы дообучить модель под свой микрофон. Сами клипы в
# репозиторий не коммитятся (см. .gitignore) — публикуется только обученная
# модель в models/.
